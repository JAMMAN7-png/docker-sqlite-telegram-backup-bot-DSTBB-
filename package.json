{
  "name": "docker-sqlite-telegram-backup-bot",
  "version": "1.0.0",
  "description": "Docker service to backup SQLite databases to Telegram",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "lint": "echo 'No linting configured yet'"
  },
  "dependencies": {
    "grammy": "^1.21.1",
    "cron": "^3.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "bun-types": "^1.0.21"
  }
}
